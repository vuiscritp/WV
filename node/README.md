# node/ — Service Puppeteer/CDP (Phase 5)

Thư mục này sẽ chứa service Node.js kết nối Puppeteer tới Chromium qua CDP
(Chrome DevTools Protocol), dùng cho chế độ `EXTERNAL_CHROMIUM` ở Phần 5 của
roadmap. Chưa có code ở Phase 1.
